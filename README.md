## Setup Instructions

- Clone the repo: git clone https://github.com/0xabdelhakim/Formation.git
- Install dependencies using uv: uv sync (requires uv installed: pip install uv).
- Launch Jupyter: jupyter notebook or use VS Code with Jupyter extension.